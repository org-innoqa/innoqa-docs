---
title: 'Ownership, code & security'
description: 'Per-project data isolation, 100% code ownership in your own GitHub repository from day one, and bring-your-own-model — on every plan.'
keywords: ["is Humainum secure", "AI website builder data security", "code ownership AI app builder", "bring your own model BYOK", "self-hostable AI app builder"]
---

These aren't upcoming features — they're how every Humainum project already works, on every plan.

<CardGroup cols={3}>
  <Card title="100% code ownership" icon="github">
    Every project gets a real GitHub repository from day one. It's already yours — clone it, run it on your own servers, or bring in another team whenever you want. No export step, no lock-in.
  </Card>
  <Card title="Per-project data isolation" icon="database">
    Every project gets its own PostgreSQL schema with scoped access. Database, GitHub and API-key access are protected per project, not shared workspace-wide.
  </Card>
  <Card title="Your keys, your terms" icon="key">
    Connect your own LLM provider credentials and control exactly which model your project's work runs on.
  </Card>
</CardGroup>

## Need something specific?

A security review, a data-processing agreement, a custom contract — [talk to us](https://inno.qa/) and we'll work through it directly.
